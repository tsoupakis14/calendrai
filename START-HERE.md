# Ξεκίνα εδώ

1. Αποσυμπίεσε το ZIP και άνοιξε τον φάκελο **calendrai-project** στο VS Code.
2. Άνοιξε το Terminal του VS Code και γράψε `npm start` (χρειάζεται Node.js 20+).
3. Άνοιξε **http://localhost:4173**.

Εναλλακτικά: χρησιμοποίησε το Live Server του VS Code. Δεν χρειάζεται εγκατάσταση dependencies και δεν πρέπει να ανοίξεις το HTML με διπλό κλικ.

**Δοκιμή:** Νέο post → γράψε τίτλο / κείμενο → Χρήση εικόνας demo → Για έγκριση → άνοιξε το post → Δοκιμή ως πελάτης → Έγκριση.

Ο κύκλος **Μ** επάνω δεξιά περιέχει την αλλαγή ρόλου και την επαναφορά των δεδομένων.

Για GitHub και Cloudflare ακολούθησε το **README.md**. Το **dist/** είναι έτοιμο στατικό build. Στο Cloudflare Pages βάλε `npm run build` και output directory `dist`.

Το πακέτο περιλαμβάνει όλο τον κώδικα, τις εικόνες και τα tests. Δεν έχει ανέβει ακόμη σε δικό σου GitHub repository. Τα δεδομένα μένουν στον browser και οι ρόλοι είναι προσομοίωση, χωρίς αληθινούς λογαριασμούς ή αποστολή αναρτήσεων στα social.
